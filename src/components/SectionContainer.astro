---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} w-full mx-auto container lg:max-w-4xl md:max-w-2xl p-4 md:p-1`}
>
  <slot />
</section>
